patterns:
  # Example: Extract the core message from a Python traceback
  - "Traceback \\(most recent call last\\):\\n.*\\n(.+)"
  
  # Example: Find a timeout error
  - "Connection timed out to host: (\\S+)"